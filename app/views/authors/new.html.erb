<h1> New Author </h1> 
<%= form_for(@author) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :add_books %>
    <%= f.text_field :books %>

    <%= f.submit %>

    <% end %> 