<h1> New Author </h1> 
<%= form_for(@author) do |f| %>
    <%= f.fields_for :authors do |a| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :add_books %>
    <%= f.text_field :books %>

    <%= f.submit %>

    <% end %> 