<h1> New Review </h1> 

<%= render partial: "layouts/errors", locals: {object: @review} %>

<%= form_for(@review) do |f| %>

    <%= f.collection_select :book_id, Book.all, :id, :title, prompt: true %> <br><br>
    <%= f.fields_for :book do |b| %> 
<% end %> 
    <%= f.label :review_title %>
    <%= f.text_field :title %>

    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.submit %>
<% end %> 